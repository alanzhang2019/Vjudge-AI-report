# 运行时依赖
flask>=3.0
openai>=1.40
jinja2>=3.1
markdown>=3.6
matplotlib>=3.8
rich>=13.0
playwright>=1.45
ddddocr>=1.5              # v3.9.52 · 密码登录自动识别图形验证码
qrcode>=7.4              # v3.8 海报二维码（漏写会导致 500）
pillow>=10.0             # qrcode 依赖 PIL 渲染 PNG
bcrypt>=4.1              # v3.10.0.2 · 学员注册密码哈希,未装则降级 PBKDF2 (admin_students.hash_password)

# 洛谷 API 依赖（pyLuogu）
httpx>=0.27
beautifulsoup4>=4.12

# .env 解析
python-dotenv>=1.0

# 可选：科学上网/加速（如不需要可删）
# -i https://pypi.tuna.tsinghua.edu.cn/simple
